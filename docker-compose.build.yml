services:
  searchproxy:
    build:
      context: .
      dockerfile: Dockerfile
    image: searchproxy:latest
    container_name: searchproxy
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
